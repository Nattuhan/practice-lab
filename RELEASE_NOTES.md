# PracticeLab v1.2.1

基本インストールを軽量化し、CPUだけで始められる解析環境、スマホ閲覧、クリックとステムの再生安定性を改善したリリースです。

## ダウンロード

- **Windows 10/11**: `PracticeLab-Setup-1.2.1.exe`
- **macOS（Apple Silicon）**: `PracticeLab-1.2.1-arm64.dmg`

Windows CPU解析、Mac解析、楽譜抽出は、アプリの「設定 → 追加機能」から必要な場合だけ追加できます。追加パックをReleaseページから手動で展開する必要はありません。

## v1.2.1の変更

### インストールと設定

- Windowsの初期解析方式をCPUに変更し、NVIDIA、WSL2、CUDAなしでも利用可能に変更
- NVIDIA GPU解析を任意の高速化オプションとして分離
- Macの曲構成解析とパート分離、Windows CPU解析、楽譜抽出を任意パックへ分離し、基本インストーラーを軽量化
- MacではApple Silicon向けの項目だけを表示し、NVIDIA・WSL設定を非表示化
- 処理履歴、ストレージ、追加機能を設定画面へ統合
- 追加機能をアプリ更新後も再利用し、不要になった旧バージョンのパックを自動整理
- Electron内蔵Node.jsの再利用、Apple SiliconネイティブFFprobe、重複動画のハードリンク化で使用容量を削減

### 再生と解析

- 動画と解析音源を同じメディアから生成し、ステム・クリックとの時間軸を統一
- シンコペーションや長い疎なイントロでも、安定区間のテンポから一定間隔のクリックを生成
- 通常再生中はステムを周期的に強制シークせず、再生開始・明示的な移動・ループ時だけ全パートを同じ時刻へ同期
- スクラブ中は動画だけを追従させ、操作が確定するまで音声を再生しないように変更
- ループ端の連続ドラッグを1画面更新ごとにまとめ、予約済みクリックを停止して多重再生を防止
- 再生開始時の重複したステム再生要求を削除
- 音切れや再同期を調べるための再生診断ログを端末内へ追加（R2や開発者には送信しません）

### R2閲覧版とスマホ

- R2公開版で曲一覧を待たずに表示し、読み込み失敗時の再試行導線を追加
- スマホでは初回に曲一覧を開き、曲選択ボタンを常時表示
- スマホの初期再生を元音源1本へ軽量化し、パート操作時だけステムを読み込み
- 設定全項目、解析、楽譜抽出、セクション編集のスマホ縦横表示を改善
- 楽譜抽出画面の小画面・横長画面での見切れを修正

## 配布される追加パック

- `PracticeLab-Windows-CPU-1.2.1.zip`
- `PracticeLab-Analysis-macOS-arm64-1.2.1.zip`
- `PracticeLab-Score-Windows-1.2.1.zip`
- `PracticeLab-Score-macOS-arm64-1.2.1.zip`
- `PracticeLab-SHA256SUMS.txt`

追加パックはアプリがSHA-256を検証してから展開します。

## 未署名版について

このリリースは費用のかかるWindowsコード署名証明書とApple Developer IDを使用していません。WindowsではSmartScreen、MacではGatekeeperの確認が表示される場合があります。詳しい起動方法はリポジトリの`UNSIGNED_DISTRIBUTION.md`を参照してください。
