# PracticeLab v1.4.1

一定テンポの曲でクリックが半分になったり、曲中だけ拍が抜けたりする問題を改善しました。

- 曲中にハーフタイムのフレーズが複数あっても、曲全体で優勢な四分音符の時計を維持
- 付点音符や短い休符区間で半テンポへ落ちた拍を、前後の一定した時計と音源から補完
- 一定テンポの曲で、弱い曲頭や余韻のある曲末までクリックを延長
- 読み上げクリックに「高め」を追加
- ボーカルだけで始まる曲でも、後続の小節頭から拍番号を逆算して曲頭から読み上げ
- 2拍小節など、解析で確定した小節ごとの拍数に合わせて読み上げをリセット

## ダウンロード

- Windows: `PracticeLab-Setup-1.4.1.exe`
- macOS（Apple Silicon）: `PracticeLab-1.4.1-arm64.dmg`

署名済みMac版とWindows版はアプリ内の更新機能から更新できます。

# PracticeLab v1.4.0

Bluetooth出力に合わせて映像と再生表示を自動補正できるようになりました。

- Macで現在のBluetooth音声出力を自動判別し、映像・波形カーソル・再生時間・セクション表示を同期
- 内蔵スピーカーでは補正せず、低速再生やループ、再生開始待ちにも追従
- 補正中のアイコンにマウスを重ねると補正量を表示し、クリックで「再生・同期」設定へ移動
- 自動・手動・オフと補正量の微調整を保存（Windowsは手動補正に対応）
- クリック音に小節の拍数に合わせた読み上げを追加
- 保存済み音声を使った再解析、拍の揺れ・欠落・半テンポ誤検出の補修を改善
- 短い2拍小節と後続の小節頭を保ち、セクション編集の境界を解析時刻に統一
- 処理一覧の閉じるアイコンと再表示矢印を修正

## ダウンロード

- Windows: `PracticeLab-Setup-1.4.0.exe`
- macOS（Apple Silicon）: `PracticeLab-1.4.0-arm64.dmg`

署名済みMac版とWindows版はアプリ内の更新機能から更新できます。

# PracticeLab v1.3.2

クリック音を練習環境に合わせて調整できるようになりました。

- クリック音を「標準」「ウッド」「ハイハット」から選択可能
- 「標準」では音程を「低め」「標準」「高め」から選択可能
- クリック音と音程の設定を保存し、次回起動時とパート音源の書き出しにも反映
- 音量設定の初期値は85%のまま、クリック音そのものを従来比1.2倍へ調整
- クリック音の設定を「設定 → 一般 → 再生」に集約

## ダウンロード

- Windows: `PracticeLab-Setup-1.3.2.exe`
- macOS（Apple Silicon）: `PracticeLab-1.3.2-arm64.dmg`

署名済みMac版とWindows版はアプリ内の更新機能から更新できます。

# PracticeLab v1.3.1

クリック解析と低速再生の同期を改善しました。

- イントロの弱い立ち上がりを含む音源でも、先頭の拍を共通の解析処理で補完
- シンコペーションを拍頭と誤認しにくい拍位置の評価へ改善
- 元音源、パート音源、クリックを一つの再生処理に統合し、低速・シーク・ループ時のずれを修正
- 低速再生でもクリック音の長さを固定し、クリックがぶつぶつと分裂して聞こえる問題を修正
- 曲名、曲ID、固定時刻に依存する専用補正を禁止し、音源と解析結果に基づく共通処理へ統一
- 再生速度、シーク、選択範囲ループを組み合わせた音声回帰検証と診断ログを追加

## ダウンロード

- Windows: `PracticeLab-Setup-1.3.1.exe`
- macOS（Apple Silicon）: `PracticeLab-1.3.1-arm64.dmg`

署名済みMac版とWindows版はアプリ内の更新機能から更新できます。

# PracticeLab v1.2.2

曲ごとの音量差を抑えるため、パート音源の生成時にも自動で音量を補正します。

## ダウンロード

- **Windows 10/11**: `PracticeLab-Setup-1.2.2.exe`
- **macOS（Apple Silicon）**: `PracticeLab-1.2.2-arm64.dmg`

Windows CPU解析、Mac解析、楽譜抽出は、アプリの「設定 → 追加機能」から必要な場合だけ追加できます。追加パックをReleaseページから手動で展開する必要はありません。

## v1.2.2の変更

### Mac版の署名とApple公証

- Mac配布アプリにDeveloper ID署名とApple公証を適用
- 署名済みMac版で更新の確認・自動ダウンロード・再起動による適用に対応
- 以前の未署名Mac版からは、今回一度DMGで入れ替えると以後はアプリ内で更新可能
- 公証票とGatekeeperの受け入れ、同梱ランタイムの起動を確認したDMGだけを公開

### パート音源の音量補正

- 全パートを合わせた音量を測定し、共通の倍率で補正
- パート間のバランスと曲中の強弱を保ち、ピークに応じて増幅量を制限
- 既存のパート音源は「再生成」で適用

## v1.2.1で追加した改善

### 全画面での練習

- PCの動画をダブルクリックすると、練習画面全体を全画面表示するように変更
- 全画面の操作列を「再生・音量・BPM／速度」の1行へ整理し、通常画面は見やすい2段を維持
- BPM操作を「半分・2倍・元に戻す・保存・裏拍」の明確な表記へ変更
- 全画面では波形・時刻・ループ情報を下部の1段へ統合し、小節数表示を省略
- タッチ端末では従来どおり、動画のダブルタップで前後へ移動
- Escキーで全画面表示を終了

### 処理一覧

- 右下の処理一覧を「−」で小さな件数バッジへ最小化可能
- 上向き矢印で一覧を再表示
- 最小化しても処理を継続し、処理履歴や結果を保持
- 最小化状態を次回起動時にも復元

## 配布される追加パック

- `PracticeLab-Windows-CPU-1.2.2.zip`
- `PracticeLab-Analysis-macOS-arm64-1.2.2.zip`
- `PracticeLab-Score-Windows-1.2.2.zip`
- `PracticeLab-Score-macOS-arm64-1.2.2.zip`
- `PracticeLab-SHA256SUMS.txt`

追加パックはアプリがSHA-256を検証してから展開します。

## 署名について

MacアプリはDeveloper ID署名・Apple公証済みです。Windows版は従来どおり未署名で、SmartScreenの確認が表示される場合があります。インストール手順はリポジトリの`UNSIGNED_DISTRIBUTION.md`を参照してください。
