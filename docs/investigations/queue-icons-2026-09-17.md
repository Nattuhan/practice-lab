# 処理一覧の右上が空欄になる問題

処理一覧のHTMLは `minus` と `chevron-up` を要求していたが、`frontend/src/vendor.js` のLucide登録に両方がなく、SVGへ置換されず空のボタンになっていた。既存テストは開閉動作だけを確認しており、アイコンの描画を検証していなかった。

利用者の指定に合わせ、畳む側は登録済みの `X` を使用し、再表示側の `ChevronUp` を登録した。処理をキャンセルせず一覧だけを最小化する動作は維持する。画面テストで実際のSVGの存在と開閉に応じた表示・非表示を確認する。

検証: Python 271件＋4サブテスト、フロント・デスクトップ単体52件、画面・再生35件。ローカルアプリは1.3.2のまま更新する。公開・同期は行わない。

ローカル更新完了。DMGは295262797 bytes、SHA-256 `a219da5198d83de87c837ad8b3f08656c992b64080189372984a413928a3e8b1`。署名、バージョン、同梱HTML/JS一致、起動とhealthzを確認。旧アプリとDMGは復元可能なゴミ箱へ移動した。
