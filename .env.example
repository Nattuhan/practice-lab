# auto は WindowsではWSL CUDA、Apple SiliconではMPS、その他ではCPUを選びます。
ANALYZER_EXECUTOR=auto
ANALYZER_DEVICE=auto
STEM_DEVICE=auto

# Cloudflare R2は利用者ごとに任意で設定します。
R2_ENABLED=0
R2_BUCKET=
R2_ENDPOINT_URL=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_PUBLIC_BASE_URL=
R2_PREFIX=sessions
